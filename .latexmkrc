# Arquivo de configuração do latexmk
# Ajusta extensões para limpeza com `latexmk -c` ou `-C`, preservando o PDF.

$clean_ext = 'acn acr alg aux bbl bcf blg glg glo gls ist lof log lot out run.xml synctex.gz toc fdb_latexmk fls';
$clean_full_ext = $clean_ext;
$cleanup_includes_cwd = 1;
